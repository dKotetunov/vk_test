<div class="line">
	<%= @user.full_name %>
	<% if @user.online %>
		<font style="float: right;">online</font>
	<% end %>
</div>

<div class="leftmain">
	<%= image_tag @user.avatar.url(:square) %>
	<%= link_to "Add to friends", add_friend_path(id: params[:id]) %>
	<%= link_to "New message", new_mess_path(id: params[:id]) %>
	<hr>
	<div>
		<div class="linehead">
			&nbsp&nbsp&nbspFriends
		</div>
	</div>
</div>

<div class="rightmain">
 <%= @user.full_name %><br>
 status...  <hr>
</div>